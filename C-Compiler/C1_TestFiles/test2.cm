/* This program contains one error, there is a missing semicolon on line 9.
This tests errors in statements.
The output should be "Error on line 10, column 3" */

/* A program to compute the factorial value of an input */

void main(void) {
  int x;
  int fac;
  x = input();
  fac = 1;

  while (x < snkfjns > 1) {
    fac = fac * x;
    x = x - 1;
  }

  output(fac);
}
